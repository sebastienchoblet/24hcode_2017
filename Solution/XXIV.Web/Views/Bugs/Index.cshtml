@model XXIV.Web.Models.ListeBugsViewModels
@{
    ViewBag.Title = "Liste des bugs";
}

<div class="jumbotron">
    @*<h1>ASP.NET</h1>
    <p class="lead">ASP.NET is a free web framework for building great Web sites and Web applications using HTML, CSS and JavaScript.</p>
    <p><a href="http://asp.net" class="btn btn-primary btn-lg">Learn more &raquo;</a></p>*@
</div>

<div class="row">
    <div class="col-md-4">
    @{
        if (Model.Bugs.Count > 0)
        {
            <h4>Liste des bugs</h4>
            <table class="table">
                <tbody>
                    @foreach (var bug in Model.Bugs)
                    {
                        <tr>
                            <td>@bug.Nom</td>
                            @*<td>
                                @if (ViewBag.ShowRemoveButton)
                                {
                                    using (Html.BeginForm("RemoveLogin", "Manage"))
                                    {
                                        @Html.AntiForgeryToken()
                                        <div>
                                            @Html.Hidden("loginProvider", account.LoginProvider)
                                            @Html.Hidden("providerKey", account.ProviderKey)
                                            <input type="submit" class="btn btn-default" value="Supprimer" title="Suppression de cette connexion @account.LoginProvider de votre compte" />
                                        </div>
                                    }
                                }
                                else
                                {
                                    @: &nbsp;
                                }
                            </td>*@
                        </tr>
                    }
                </tbody>
            </table>
            }
        }
    </div>
    <div class="col-md-4">
      
    </div>
    <div class="col-md-4">
      
    </div>
</div>